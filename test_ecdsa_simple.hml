import { ecdsa_generate_key } from "@stdlib/crypto";

print("Testing ECDSA key generation");
print("Step 1: Calling ecdsa_generate_key()");

try {
    let keypair = ecdsa_generate_key();
    print("Step 2: Key generation succeeded!");
    print("Keypair type: " + typeof(keypair));
} catch (e) {
    print("Step 2: Key generation failed: " + e);
}
