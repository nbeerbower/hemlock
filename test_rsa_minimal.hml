print("Test 1: Before import");
import { rsa_generate_key } from "@stdlib/crypto";
print("Test 2: After import");

// Temporarily replace the function with a minimal version
fn rsa_generate_key_minimal() {
    print("[MINIMAL] Function entered");
    let keypair = {
        private_key: null,
        public_key: null
    };
    print("[MINIMAL] Returning empty keypair");
    return keypair;
}

print("Test 3: Calling minimal version");
let result = rsa_generate_key_minimal();
print("Test 4: Minimal version succeeded");
print("Result type: " + typeof(result));

print("Test 5: Now calling actual rsa_generate_key");
let keypair = rsa_generate_key();
print("Test 6: Actual version succeeded");
print("Keypair type: " + typeof(keypair));
